{{#each finalSteps}}
<div class="hide-element step" data-id="hidden-element">
    <div class="step-index">
        <h2>{{number}}</h2>
    </div>
    <div class="step-content">
        <h2>{{title}}</h2>
        <p>
            {{description}}
        </p>
    </div>
</div>
{{/each}}